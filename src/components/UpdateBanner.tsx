import React from 'react';

/*
  Banner de "nova versão disponível" (atualização do service worker).
  No app original escuta o evento de update do PWA. Inerte aqui — mantido
  para o App.tsx seguir idêntico ao blueprint.
*/
const UpdateBanner: React.FC = () => null;

export default UpdateBanner;
